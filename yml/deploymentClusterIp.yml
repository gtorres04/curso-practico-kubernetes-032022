apiVersion: apps/v1
kind: Deployment
metadata:
  name: mideploymentclusterip
  namespace: paradigma
spec:
  strategy:
    type: RollingUpdate
  replicas: 3
  selector:
    matchLabels:
      app: miappclusterip
  template:
    metadata:
      labels:
        app: miappclusterip
    spec:
      containers:
        - image: nginx
          name: nginx
          ports:
          - containerPort: 80